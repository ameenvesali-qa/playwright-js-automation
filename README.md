an E2E test suite for the SauceDemo practice site, built with playwright and JS.

These tests cover Login(including success, wront password, non-user login) and adding an item to the cart

How to Run:
     npm install
     npx playwright install chromium
     npx playwright test

